<h1 class="page-header">Blog - Categorias</h1>
<div class="panel panel-default" >
    <div class="panel-heading">
        Listando Categorias <span class="badge badge-light"><%= @contador.count %></span>
        <%= link_to new_backoffice_category_path, class: "btn btn-success pull-right", style: "margin-top: -5px" do %>
            Novo Cadastro
        <% end %>
    </div>
    <!-- /.panel-heading -->
    <div class="panel-body">
        <div class="table-responsive">
            <table class="table table-striped table-bordered table-hover">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Nome</th>
                        <th>Qnt. de posts</th>
                        <th colspan="2">Opções</th>
                    </tr>
                </thead>
                <tbody>
                    <% @categories.each do |category| %>
                        <tr>
                            <td width="3%"><%= category.id %></td>
                            <td><%= category.name %></td>
                            <td><%= category.posts.count %></td>
                            <td width="3%">
                                <%= link_to edit_backoffice_category_path(category.id), class: "btn btn-primary btn-circle" do %>
                                    <i class="fa fa-edit"></i>
                                <% end %>
                            </td>
                            <td width="3%">
                                <%= link_to backoffice_category_path(category.id), class: "btn btn-danger btn-circle", data: {confirm: "Você realmente deseja excluir (#{category.name})?"}, method: :delete do  %>
                                    <i class="fa fa-trash"></i>
                                <% end %>
                            </td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
        </div>
        <!-- /.table-responsive -->
        </p> <center><%= paginate @categories %></center>
    </div>
    <!-- /.panel-body -->
</div>

