$(function(){
  // Hides the Modal
  $('#sendMailModal').modal('hide');

  // Set subject and message to empty string
  $('#recipient-text').val('');
  $('#subject-text').val('');
  $('#message-text').val('');

  // Show Message
  $.notify("Mensagem Enviada!", "success")
});